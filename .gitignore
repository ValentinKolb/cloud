# OS / metadata
.DS_Store
.claude
./reviews
**/moodboard

# Pre-modularization reference material, kept on disk but not versioned
_old/

# Local tooling state (skill task tracking, lint cache)
.dex/
.fallow/

# Local scratch notes (todos, draft reviews)
todo/

# Per-developer seed scripts (hardcoded user UUIDs etc.)
scripts/seed-dashboard-demo.ts

# Widgets parked while the runtime widget-fetch mechanism is being designed
_disabled-widgets/

# Environment / local config
.env
.env.*
!.env.example

# Dependencies
node_modules
**/node_modules

# Build artifacts
dist
**/dist
build
**/build
_ssr
**/_ssr
/public

# Generated CSS
**/public/build.css

# Logs / caches
*.log
.cache
**/.cache
