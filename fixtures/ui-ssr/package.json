{
  "name": "@k2b/ui-ssr-fixture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run --cwd ../../packages/ui build && bun run scripts/build.ts",
    "dev": "NODE_ENV=development bun --preload ./src/preload.ts ./src/server.ts",
    "test": "bun run --cwd ../../packages/ui build && bun test",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false"
  },
  "dependencies": {
    "@k2b/ssr": "0.12.0",
    "@k2b/ui": "workspace:*",
    "hono": "^4.11.1",
    "solid-js": "^1.9.10"
  },
  "devDependencies": {
    "@types/bun": "1.3.9",
    "typescript": "^5.9.3"
  }
}
