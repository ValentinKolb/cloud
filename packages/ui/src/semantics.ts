/** Semantic intent for user-facing feedback and actions. */
export type IntentTone = "neutral" | "info" | "success" | "warning" | "danger";

/** Deliberately small accent palette for data and identity presentation. */
export type AccentColor = "zinc" | "blue" | "emerald" | "amber" | "red";
