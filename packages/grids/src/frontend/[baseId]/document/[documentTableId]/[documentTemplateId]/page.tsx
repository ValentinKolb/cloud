/**
 * Document template workspace page for
 * `/app/grids/<base>/document/<table>/<template>`.
 * The base-level handler owns SSR state loading and route branching.
 */
export { default } from "../../../page";
