# Dev containers — run with:
#   bun run dev          (core set, 6 containers)
#   bun run dev:full     (core + extras, 19 containers; --profile extra)
#   bun run dev:app FOO  (ad-hoc: start a single extra app in the running stack)
#
# Requires infra running: bun run infra
#
# Gateway replaces Traefik — routes are discovered dynamically from the Redis
# app registry, so ad-hoc containers joining the same docker network (= same
# compose project name = folder name "cloud") are picked up automatically.

x-env: &env
  NODE_ENV: development
  DATABASE_URL: postgresql://ipa:ipa@ipa_postgres:5432/ipa
  REDIS_URL: redis://ipa_valkey:6379
  APP_SECRET: dev-secret-change-me-in-production

x-app: &app
  build:
    context: .
    dockerfile: Dockerfile.dev
  working_dir: /app
  restart: unless-stopped

services:
  # ══════════════════════════════════════════════════════════════════
  # Core set — started by `bun run dev`
  # ══════════════════════════════════════════════════════════════════

  gateway:
    <<: *app
    container_name: gateway
    environment: { <<: *env, APP_ID: gateway }
    ports:
      - "3000:3000"
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/gateway/src:/app/packages/gateway/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/gateway/src/index.ts

  app-core:
    <<: *app
    container_name: app-core
    environment: { <<: *env, APP_ID: core, ADMIN_LOGIN_TOKEN: dev-admin }
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/cloud/scripts:/app/packages/cloud/scripts
      - ./packages/cloud/public:/app/packages/cloud/public
      - ./packages/core/src:/app/packages/core/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/core/src/index.ts

  app-accounts:
    <<: *app
    container_name: app-accounts
    environment: { <<: *env, APP_ID: accounts }
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/accounts/src:/app/packages/accounts/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/accounts/src/index.ts

  app-logging:
    <<: *app
    container_name: app-logging
    environment: { <<: *env, APP_ID: logging }
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/logging/src:/app/packages/logging/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/logging/src/index.ts

  app-settings:
    <<: *app
    container_name: app-settings
    environment: { <<: *env, APP_ID: settings }
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/settings/src:/app/packages/settings/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/settings/src/index.ts

  app-notifications:
    <<: *app
    container_name: app-notifications
    environment: { <<: *env, APP_ID: notifications }
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/notifications/src:/app/packages/notifications/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/notifications/src/index.ts

  # ══════════════════════════════════════════════════════════════════
  # Extras — only started with `--profile extra` or ad-hoc via dev:app
  # ══════════════════════════════════════════════════════════════════

  app-notebooks:
    <<: *app
    container_name: app-notebooks
    environment: { <<: *env, APP_ID: notebooks }
    profiles: [extra]
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/notebooks/src:/app/packages/notebooks/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/notebooks/src/index.ts

  app-contacts:
    <<: *app
    container_name: app-contacts
    environment: { <<: *env, APP_ID: contacts }
    profiles: [extra]
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/contacts/src:/app/packages/contacts/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/contacts/src/index.ts

  app-faq:
    <<: *app
    container_name: app-faq
    environment: { <<: *env, APP_ID: faq }
    profiles: [extra]
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/faq/src:/app/packages/faq/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/faq/src/index.ts

  app-files:
    <<: *app
    container_name: app-files
    environment: { <<: *env, APP_ID: files, FILEGATE_URL: "http://filegate:4000", FILEGATE_TOKEN: dev-secret-token }
    profiles: [extra]
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/files/src:/app/packages/files/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/files/src/index.ts

  app-ipa-hosts:
    <<: *app
    container_name: app-ipa-hosts
    environment: { <<: *env, APP_ID: ipa-hosts }
    profiles: [extra]
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/ipa-hosts/src:/app/packages/ipa-hosts/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/ipa-hosts/src/index.ts

  app-oauth:
    <<: *app
    container_name: app-oauth
    environment: { <<: *env, APP_ID: oauth }
    profiles: [extra]
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/oauth/src:/app/packages/oauth/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/oauth/src/index.ts

  app-proxy-auth:
    <<: *app
    container_name: app-proxy-auth
    environment: { <<: *env, APP_ID: proxy-auth }
    profiles: [extra]
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/proxy-auth/src:/app/packages/proxy-auth/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/proxy-auth/src/index.ts

  app-quotes:
    <<: *app
    container_name: app-quotes
    environment: { <<: *env, APP_ID: quotes }
    profiles: [extra]
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/quotes/src:/app/packages/quotes/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/quotes/src/index.ts

  app-spaces:
    <<: *app
    container_name: app-spaces
    environment: { <<: *env, APP_ID: spaces }
    profiles: [extra]
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/spaces/src:/app/packages/spaces/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/spaces/src/index.ts

  app-tools:
    <<: *app
    container_name: app-tools
    environment: { <<: *env, APP_ID: tools }
    profiles: [extra]
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/tools/src:/app/packages/tools/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/tools/src/index.ts

  app-ui-lab:
    <<: *app
    container_name: app-ui-lab
    environment: { <<: *env, APP_ID: ui-lab }
    profiles: [extra]
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/ui-lab/src:/app/packages/ui-lab/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/ui-lab/src/index.ts

  app-weather:
    <<: *app
    container_name: app-weather
    environment: { <<: *env, APP_ID: weather }
    profiles: [extra]
    volumes:
      - ./packages/cloud/src:/app/packages/cloud/src
      - ./packages/weather/src:/app/packages/weather/src
      - ./styles.css:/app/styles.css
    command: bun run --preload=/app/packages/cloud/scripts/preload.ts --watch packages/weather/src/index.ts
